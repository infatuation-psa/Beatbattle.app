{{ define "Footer" }}
<footer><button class="chip dark-toggle">dark mode</button><a class="footer-icon" href="https://twitter.com/PSAInfatuation"><img src="/static/img/twitter-logo.svg" /></a></footer>
    {{if index . 1}}<script>$(document).ready(function() { M.toast({ html: {{index . 0}}, classes: {{index . 1}} }); });</script>{{end}}
      <script type="text/javascript" src="/static/materialize.min.js"></script>
      <script>const darkmode = new Darkmode();</script>
      <script>
      if (darkmode.isActivated()) {
        $(".dark-toggle").html("🌕 dark mode");
      }
      else {
        $(".dark-toggle").html("🌑 dark mode");
      }
      $(document).ready(function() {
        $(".dark-toggle").click(function() { 
          darkmode.toggle(); 
          if (darkmode.isActivated()) {
            $(".dark-toggle").html("🌕 dark mode");
            $(".material-icons[style='color: black;']").css("color", "white")
          }
          else {
            $(".dark-toggle").html("🌑 dark mode");
            $(".material-icons[style='color: white;']").css("color", "black")
          }
        });
      });
      </script>
    </body>
</html>
{{ end }}